#include <stdio.h>

int main(){
    int a = 4;
    int b = 3;
    int c = a + b ;
    printf("The value of a is %d and the value of b is %d and sum is %d\n",a,b,c);
    //Modulus operator is used to find the reminder .
    printf("The reminder when a is divided by b is %d\n",a%b);

    // this does not work for expontiation in c
    // int d = a^b;
 return 0;
}