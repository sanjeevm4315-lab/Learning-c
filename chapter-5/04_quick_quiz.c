#include <stdio.h>
#include <math.h>

int main()
{
    // Use the library function to calculate the area of a square with side a.
    //  Solve this in c language

    float a =5;
    printf("The area of the square with side %f is %f\n", a, pow(a, 2));
    return 0;
}